{
  "name": "nestwise",
  "version": "0.1.0",
  "private": true,
  "description": "NestWise — track personal finances, plan retirement, get savings advice",
  "scripts": {
    "install:all": "npm install && npm --prefix client install",
    "dev": "concurrently -n convex,web -c green,magenta \"npm --prefix client run convex\" \"npm --prefix client run dev\"",
    "start": "concurrently -n convex,web -c green,magenta \"npm --prefix client run convex\" \"npm --prefix client run dev\"",
    "convex": "npm --prefix client run convex",
    "client": "npm --prefix client run dev",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
